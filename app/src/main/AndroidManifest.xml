<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s525189.eventtracker">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BackendApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="Bearcat's Event"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".homeActivity"
            android:label="Home" />
        <activity android:name=".ProfileActivity"
        android:label="Profile" />
        <activity android:name=".AdminActivity" android:label="Admin" />
        <activity android:name=".EventSubmitActivity"  />
        <activity android:name=".MainActivity" android:label="Event Details" />
        <activity android:name=".description"/>
        <activity android:name=".AdminLogin" android:label="Admin Login" />
        <activity android:name=".uploadimg"></activity>
        <activity android:name=".LoginActivity"  android:label="Login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" android:label="Register" />
        <activity android:name=".ResetPassword" android:label="Reset Password" ></activity>
    </application>

</manifest>